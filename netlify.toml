[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback (на случай перехода с HashRouter на BrowserRouter)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
