{
  "permissions": {
    "deny": [
      "Bash(rm -rf*)",
      "Bash(*rm -rf*)",
      "Bash(git push --force*)",
      "Bash(git push -f*)",
      "Bash(rmdir /s*)",
      "PowerShell(Remove-Item*-Recurse*)",
      "PowerShell(*Remove-Item*-Recurse*)",
      "PowerShell(rd /s*)",
      "PowerShell(git push --force*)",
      "PowerShell(git push -f*)"
    ]
  }
}
